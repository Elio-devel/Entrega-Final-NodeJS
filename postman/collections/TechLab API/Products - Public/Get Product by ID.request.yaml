$kind: http-request
name: Get Product by ID
method: GET
url: '{{baseUrl}}/api/products/{{productId}}'
order: 2000
description: Returns a single product by its Firestore document ID.
auth:
  type: noauth
