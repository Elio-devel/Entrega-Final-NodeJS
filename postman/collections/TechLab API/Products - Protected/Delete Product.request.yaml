$kind: http-request
name: Delete Product
method: DELETE
url: '{{baseUrl}}/api/products/{{productId}}'
order: 3000
description: Deletes a product by its Firestore document ID. Requires a valid JWT token.
